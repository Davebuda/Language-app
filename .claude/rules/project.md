---
description: norskcoach project-specific conventions.
paths:
  - "**/*.tsx"
  - "**/*.css"
---

# norskcoach — Conventions

- Framework: Next.js | Runtime: node
- TypeScript strict; no `any` without a comment.
- Follow existing directory layout and import aliases.

Global web / frontend / supabase / deploy standards load automatically from `~/.claude/rules/`.
