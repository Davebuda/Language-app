node_modules/
.next/
.env.local
.env.*.local
*.tsbuildinfo

# Generated audio (deploy to VPS, not git)
public/audio/sentences/

# Scout working files (lane temp files — keep briefs and config)
.scout/lane-*.md
.scout/planner.md

# Harness reports (generated, not committed)
reports/

# OMC state (agent orchestration)
.omc/

# Playwright MCP artifacts
.playwright-mcp/

# Scratch / working files under output/ (logs, temp data, throwaway scripts).
# Intentional docs live as output/<name>.md and stay tracked.
output/_*

# Council devserver log
.council/*.log

# Build logs
.next-logs/

# Test results (generated)
test-results/

# Audit screenshots
audit-*.png

# Pre-seed staging drafts (never shipped; live content is content/<area>/<level>.json)
content/**/staging/

# Scheduler lock (local session state)
.claude/scheduled_tasks.lock
