# NorskCoach — Project Context for Claude Code

## What This Is

An adaptive Norwegian language learning web app. Not a course — a coach. It builds a per-user mistake fingerprint, generates a personalized session every time, and runs every wrong answer through a repair loop with spaced-repetition follow-up.

**One-line positioning:** A personal Norwegian tutor that finds your weak spots, explains why they happen, and fixes them with targeted practice — and pushes you to actually speak.

This file is the single source of truth for how to work on this project. If anything you believe about the project conflicts with this file, this file wins. If this file conflicts with observed code, stop and flag it — do not silently work around it.

## The Moat

Three things working together. Everything serves these:

1. **Diagnosis** — knowing precisely what the learner is failing at, including root causes under surface mistakes.
2. **Scheduling** — generating a personalized session that mixes remediation, review, new material, and interleaving.
3. **Remediation** — every wrong answer triggers explain → micro-drill → retry → schedule for spaced review.

External research (see `docs/validation-and-research.md`) confirmed this combination is genuinely differentiated against Lingu, Babbel, Busuu, and the Norskprøven prep ecosystem. No competitor does root-cause diagnosis. The moat is the diagnostic coaching intelligence — not the SRS, not the exercise types, not the model.

AI is a power tool that supports the coaching. AI is never the headline. Every AI path has a non-AI fallback.

## The North Star (Results Principle)

The app exists to make the user speak more Norwegian and build sentences confidently. Two layered outcomes: production fluency (can construct sentences) and speaking confidence (actually does, in low-stakes varied contexts). Every feature must push toward production. A pure-recognition feature fails this test unless wrapped with an output requirement.

The "Norwegian dominates the screen" principle applies to learning surfaces (session loop, translation, fill-in-blank, word-order, journal, muntlig). It does not apply to assessment surfaces (diagnostic, recalibration) where English instructions with Norwegian options are correct by design — instruction clarity matters more than immersion when measuring placement.

## Tech Stack (ACTUAL — not aspirational)

- **Platform:** Web. Next.js (App Router), TypeScript strict, Tailwind.
- **Backend:** Supabase (Postgres + Auth + RLS).
- **State:** Zustand stores; fingerprint persists to IndexedDB, syncs to Supabase fire-and-forget for auth users.
- **AI (hybrid):** Desktop: WebLLM in a Web Worker via WebGPU (Llama-3.2-1B-Instruct, ~400MB, client-side). Mobile: Groq API via `/api/ai` server route (Llama 3.1 8B, server-side, requires `GROQ_API_KEY`). Fallback: template/rule responses when neither path is available. Profile page shows current AI mode (Lokal/Sky/Maler). Core engine does NOT depend on AI — every AI path has a non-AI fallback. Future upgrade: NB-Llama (Norwegian-fine-tuned) for both paths.
- **Typography:** Schibsted Grotesk (single family, display 700 / body 400). Designed by Schibsted (Norwegian media) for Scandinavian digital reading; æ/ø/å are primary design requirements.
- **Hosting target:** small EU VPS + Supabase EU region. Free per user is a hard constraint.

There is no iOS app, no Swift, no MLX, no SwiftUI. Earlier design docs describing those are historical artifacts from a deferred native plan. Do not act on them.

## Current State (live-bug + moat-visibility sweep 2026-06-05/06; core loop RUNTIME-VERIFIED on HEAD 2026-06-03)

> **2026-06-27 update — speaking-input fix: continuous listening + learner-controlled finish, SHIPPED + DEPLOYED to pandoai.no (runtime HEAD `105eea5`; `audit:gate` AUDIT-CLEAN 970 tests + Rule-9 reconciled + live HTTP smoke).** The Web Speech recognizer in the speaking exercises cut learners off: the shared hook ran `continuous = false` (browser endpointing finalized on the first pause) and **Lytt og svar** + **Rollespill** capped listening at a hard `LISTEN_SECONDS = 5` — a struggling learner who paused to think ran out the clock with nothing captured. Fix: `useSpeechRecognition` (`src/hooks/useSpeechRecognition.ts`) gained an opt-in `continuous` flag (default `false` — Recite/Drill one-shot pronunciation checks unchanged). **Lytt og svar** (`ListenRespondExercise.tsx`) + **Rollespill** (`RoleplayScreen.tsx`) opt in: window 5→**20s**, a **"Ferdig"** button to finish early, trailing interim words folded into the capture, `stop()` on timer expiry, auto-advance demoted to an unexpected-end safety fallback. **Shadowing** (`ShadowingExercise.tsx`) opts into continuous only (manual tap-to-stop already existed). **Honesty (Rule 8):** Rollespill reused `LISTEN_SECONDS` to credit speaking-minutes — split into a separate `ESTIMATED_TURN_SECONDS = 5` so the larger window can't over-credit minutes a learner who tapped Ferdig never spoke. NOT runtime-verified on-device (Web Speech `continuous`, esp. Android Chrome, is browser-only and not exercisable in CI — final proof is an on-device pause-and-finish pass). Decision: `docs/decisions/2026-06-27-speaking-exercises-continuous-listening.md`.

> **2026-06-26 update — Kari calibration + 5 audit fixes + G-03 + Plan C (T1.6) SHIPPED + DEPLOYED to pandoai.no (runtime HEAD `6ef0132`; two Rule-9 deploy passes `b09ff69` then `6ef0132`, every commit `audit:gate` AUDIT-CLEAN + live-verified).** A long session: "make Snakk med Kari helpful" → Recourse → Investigate → Solve+Council → ship. Live + verified:
> 1. **5 audit-backlog fixes** (`e002dbd`,`17e821c`,`c5dbdc3`,`61a4fe1`,`cd09caa`): **E-01** `/eval` guarded out of prod (server-component `notFound()` when `NODE_ENV==='production'` — live 404; closed an unauth Groq-spend leak); **R-02** `/reading` honest below-level banner for B1/B2 (`getReadingContentLevel`, mirrors roleplay/listen); **AI-01** journal "Rettet versjon" now weaves ONLY gate-CONFIRMED corrections (pure `buildCorrectedText` lib + caveat) — show-don't-grade extended to the displayed text; **D-01** onboarding AWAITS the diagnostic seed before navigating (`buildSeededFingerprint` extracted) — closed a fast-click level-reset-to-A1 race; **S-05** unresolvable session item → honest "Hopp over" `UnresolvedItemCard` (advance only, NO fingerprint write) not an infinite skeleton.
> 2. **Kari conversation calibration** (`b09ff69`): evidence-driven (8 live API probes + a 4-model bake-off + Playwright). Routed the Kari turn to **`llama-3.3-70b-versatile`** (new `CONVERSATION_MODEL` env, default 70B; 8b stays for the high-volume JSON generation path = free-tier budget), temp 0.85→0.6, rewrote `buildConversationPrompt` (anti-echo — never repeat the learner's mistake; exactly ONE follow-up question; level-true vocab; natural register "rutinen din" not "din rutine"; English-redirect; dropped the leaky `[brackets]` Rule 6), hardened `stripTutorScaffolding`. The old `llama-3.1-8b-instant` produced non-native Bokmål, echoed the learner's errors, and emitted self-corrections; 70B is near-native AND reliably flags real learner errors. Override with `CONVERSATION_MODEL` if the 70B free-tier ceiling bites.
> 3. **G-03** (`19de089`): 162 B1/B2 `translate-EN` `accepted_answers` (30/34 B1, 132/133 B2), conservative "a false-accept is worse than a false-reject" contract + a new integrity gate. Kills the chronic B1/B2 exact-match-fail → self-attest → correct-at-HALF-weight muddying of the diagnosis input.
> 4. **Plan C / T1.6 DONE** (`6ef0132`, `src/engine/scheduler.ts`): diagnosis now steers the snakk + new-material pools (the architect-redefined moat-safe scope). **①** Snakk block LEADS with the diagnosed root cause + affected concepts and labels the root-cause speaking item `root_cause` (you speak the construction that breaks your sentences). **②** new-material modality tilts to the diagnosis focus via `resolveNewMaterialFocus` with a **cold-start guard** — never force production output on a zero-attempt concept (i+1 scaffolding). **Review/SRS-due stay diagnosis-AGNOSTIC** — locked boundary test (an unrelated SRS-due concept still schedules under a strong off-target diagnosis; the ladder is untouched).
> 5. **Council killed a bad build (process win):** the Solve→Council loop on "feed Kari's V2 catches into the moat" rejected a proposed deterministic V2/word-order verifier BEFORE any code — a Rule-8 false positive (`"Kanskje jeg kommer"→"Kanskje kommer jeg"` is valid Norwegian) + near-zero real coverage (AI corrections are minimal spans `"jeg er"→"er jeg"`, not full clauses). **V2-from-conversation stays show-don't-grade; the NB-Llama deferral holds.** This is why the moat got *cleaner* (G-03) + *broadcast wider* (Plan C) instead of a new poison vector.
>
> **2026-06-24 update (later) — dashboard redesign: moat-led home + always-visible layout + count-leads command card, SHIPPED + DEPLOYED to pandoai.no (runtime HEAD `a815be6`; commits `345459e`,`0028d8d`,`a815be6` on top of the notebook ship; each `audit:gate` AUDIT-CLEAN + Rule-9 reconciled + live-prod-verified at 375px).** A user-directed `/redesign-orchestrator` pass (full reasoning trail under `.omc/redesign/dashboard/`: brief, references, design-axes, 5 explored directions, the approved mockups, report). Shipped + live:
> 1. **The home leads with the diagnostic moat** (`345459e`): LingQ-style word-state tinting (`src/lib/word-state.ts` — pure unknown/saved/known derived off the notebook, no SRS/mastery; shared by `/reading` word tints + notebook state-dots), a toast visibility layer (`src/stores/toast-store.ts` + `src/components/ui/ToastViewport.tsx`, mounted in layout), a new `/snakk` speaking hub, and Dirigenten IA on `BottomNav` (home · session · snakk · notatboka · deg). Concept labels translated EN→NO across all four CEFR graphs (`0028d8d`).
> 2. **Command card decluttered to "count-leads"** (`a815be6`, `src/components/dashboard/ProductionWall.tsx`): the dark STATUS half no longer stacks 5 competing blocks. It leads with the production count; the breaker is a tight **"Fanget · {label}"** chip with an HONEST `prior → this` decline (green only on a real drop — Rule 6/8); the diagnosis folds to one quiet line; a cream **"Fikset"** proof chip is driven by real `deriveBreakerStory().retired` (empty → no chip; honest cold-start gate). New `fixedLabels` prop. The old shrinking-gauge verdict block + duplicate objective title + 4-chip diagnosis row are gone.
> 3. **Everything is visible — the "Mer øving og status" collapse is REMOVED** (`a815be6`, `src/app/dashboard/page.tsx`): four always-visible sections with varied surfaces (cream lane list → cream Muntlig 3-tile grid → dark Notatboka row → cream stat strip + dark week overview). The coach-recommended lane leads with a lime **"Anbefalt"** highlight (`LaneTrackRow` gains a `recommended` prop). The standalone week-bar block was dropped (its count moved to the Øving header). **This intentionally REVERSES the T1.1 "collapse the menu behind one tap" choice — by explicit user direction** (show everything, design it well). Decision: `docs/decisions/2026-06-24-dashboard-expression-count-leads.md`.
> 4. **Bøyningsdrill (`/ord`) surfaced at ALL levels** (`a815be6`): it draws from a fixed everyday-verb pool (irregular-first, `src/lib/conjugation-drill.ts` → `VOCAB_WORDS`) — foundational, not B2-only — so it's now a tracked daily lane in "Øving · dagens plan" for every level (`coreLanes` denominator 5→6). The "B2 · ordforråd" framing on the drill intro + `/ord` route metadata was genericized so it's honest below B2.
> 5. **Design law (confirmed by the user this session, saved to memory `feedback-design-density`):** cream/white (`#F0F1EC`) is a MAIN canvas color in the **dark / lime / cream** blocking system — NOT an accent. An all-dark card exploration was rejected as "ugly"; favor cream-led/editorial surfaces and real dark↔light balance, and do not under-weight cream when exploring designs.
>
> **2026-06-24 update — interactive corrections + universal learner notebook SHIPPED + DEPLOYED to pandoai.no (runtime HEAD `b35cca2`; 9 commits `47f7a7d..b35cca2`, every one `audit:gate` AUDIT-CLEAN + Rule-9 reconciled + visual & live-prod-smoke QA'd).** A new product layer (direction: `docs/direction-interactive-corrections-and-notebook.md`; plan: `docs/plan-corrections-and-notebook.md`). Shipped + live:
> 1. **2nd-person feedback** (`ad267fb`): the 5 AI prompt builders (`src/ai/prompts.ts`), diagnosis coach lines (`src/engine/diagnosis.ts`), and constraint eval (`src/lib/constraints.ts`) now address the learner directly as "du/you", never "the learner".
> 2. **Clickable word-selection popup** (`src/components/shared/WordPopup.tsx` + `SavableWord.tsx` + the `src/lib/word-explanation.ts` resolver + `src/components/ui/popover.tsx` [new Radix dep]): every corrected word in reading/conversation/journal/repair is tappable → verified-corpus rule+gloss as PRIMARY, AI explanation marked "Forslag" (secondary), honest "Ingen oppslag ennå" when neither (AI-01 — never fabricates a translation). Save-from-anywhere via one shared component.
> 3. **`/vocab` is now the universal learner notebook** (NOT a "Kommer i versjon 2" stub): `NotebookScreen` + `useNotebook` hook + `notebook-store` (Zustand) + a `notebook-items` IndexedDB store (DB bumped v1→v2, fingerprints preserved) + a `learner_notebook` Supabase table (row-per-item, RLS "User owns notebook"). Guest-capable IndexedDB-first; Supabase sync for auth users (`if (user)` gated, mirrors `fingerprint_sync`); anon→auth migration re-keys saved items on login.
> 4. **"The word comes back"** — promoted notebook items (HYBRID gate: explicit "Øv på dette" OR diagnostic relevance, `src/lib/notebook-promotion.ts`) re-enter the daily økt as deterministic `translation-to-norwegian` (scheduler injects ≤2, new-material lane ONLY — T1.6 review/SRS-due untouched) on their OWN SRS ladder (`src/lib/srs.ts` `advanceNotebookSrs`), graded via the existing S-01 `fallbackContent` path. **`submitResult` (`src/hooks/useSession.ts`) BRANCHES: a `notebook_practice` result advances ONLY the item's own SRS and NEVER reaches `recordFingerprintResult`/`updateConceptMastery`** — the "no unverified AI moves mastery" invariant + T1.6 hold. Decision: `docs/decisions/2026-06-24-notebook-deterministic-recall.md`.
> 5. **Verified, not assumed:** fresh-context code review (5 fixes incl. a guest→login silent-data-loss bug); Playwright visual QA; real-IndexedDB integration tests via `fake-indexeddb` (v1→v2 upgrade non-destructive / SRS round-trip / migration); live prod smoke on pandoai.no (word-click→save→IndexedDB, v2 store created alongside fingerprints). The one not-runtime-driven piece: a live *authed* Supabase round-trip (OTP blocks automation) — covered by table+RLS verification + auth-path unit test + fingerprint-sync parity.
>
> **2026-06-22 update (later) — deep principal-QA audit + T1.4 + 5 audit fixes, all DEPLOYED to pandoai.no (runtime HEAD `0d598c3`; every commit `audit:gate` AUDIT-CLEAN + Rule-9 reconciled + visual-QA'd where UI changed).** A user-invoked principal-QA prompt drove a 7-phase read-only audit (living files in **`audit/`**: system-map, live-audit-log, issue-registry, root-cause-map (10 clusters), diagnostic-plan, fix-plan, regression-checklist). 5 parallel cluster agents + own corpus/grader verification. **Key finding: MOST of the 13 reported issues were already fixed** — diagnostic explanation off-by-one, mic auto-start-without-consent, WebLLM gibberish, SSR/CSR level mismatch, journal mastery-poisoning, seed-path retry are all **refuted/already-fixed** in current code; the `confirmedRepair` "no unverified AI moves mastery" invariant holds. Re-verify, don't re-report (full confirmed-fixed table in `audit/issue-registry.md`). Shipped + live this session:
> 1. **T1.4 — name-the-root-cause in-session** (`4fc8e58`): added a `root_cause` `SelectionReason`; the scheduler tags ONLY the diagnosed `rootCauseConceptId` remediation item (precedence over weekly_focus/weak_concept; symptom concepts keep their reason); in-session badge "Grunnårsak" + justification "Dette ligger bak flere av feilene dine". The visible half of `edcd54d`. (Tier-1 T1.6 — diagnosis-steers-ALL-pools — REDEFINED by the architect to a moat-safe 2-pool scope: steer new-material modality + snakk concepts only; **review/SRS-due stay diagnosis-agnostic** because tilting spaced-repetition by today's diagnosis breaks the 1→3→7→14→30 ladder. Not yet built.)
> 2. **A-01 (CRITICAL) — B2 fill-in-blank was systematically unwinnable** (`82fc798`): `deriveCorrectAnswer` grades fill-in-blank `notes` VERBATIM, but 182/184 B2 rows stored `notes` as `"answer — explanation"` → every B2 fill-in-blank marked a correct answer wrong → phantom error + SRS reset + poisoned diagnosis (A1/A2/B1 were clean). Migrated B2 `notes` to the bare answer; embedded `/` alternatives → `accepted_answers`. Grader/component unchanged. New CI gate (`tests/content/fill-in-blank-integrity.test.ts`) rejects annotation-shaped notes (em-dash/quote/semicolon/slash).
> 3. **S-01 (High) — AI-generated content froze the grader** (`f4b8521`): `gradeAnswer` resolved ids only against corpus/Supabase → generated content (built client-side) resolved nowhere → null → exercise silently froze (no-advance trap on the moat's remediation pillar). Added optional `fallbackContent` on `gradeAnswer`; the 3 server-graded components pass the content they already hold when `source==='generated'`. Seed grading byte-for-byte unchanged; `classifyError`/`POS_MAP` stay server-side. (Design note: the line-84 "answer not exposed to client" rationale was already void — the full sentence map incl. `norwegian` ships to the browser.)
> 4. **R-01 (High) — orphaned muntlig lanes** (`afe282b`): `/listen`,`/drills`,`/shadow` were built + fingerprint-wired but had no in-app entry point. Added a "Muntlig" sub-panel (Lytt/Uttale/Skygging via `LaneTrackRow`) INSIDE the dashboard "Mer øving og status" disclosure (conductor hero stays one prescribed action; supplementary, not in the daily denominator). The CLAUDE.md "Muntlig section links to all three" claim is now TRUE.
> 5. **G-02 + S-02 (High + Medium) — non-punitive speed-round** (`0d598c3`): a speed-round MISS (valid English paraphrase / speed typo / EMPTY timeout submission) no longer enters the repair loop, resets SRS, or logs a fingerprint error — speed-round is rapid recall graded by brittle exact-match English, so a "wrong" is unreliable. `submitResult` exempts speed-round misses; the component reveals the canonical answer + "Neste" instead of routing to repair. Correct still bricks. Decision: `docs/decisions/2026-06-22-non-punitive-speed-round.md`.
>
> **Remaining audit backlog (all Medium/Low, triaged in `audit/issue-registry.md`):** S-03 (repair-cap silent swallow), S-04 (skip=correct inflates accuracy), S-05 (unresolved-item infinite-skeleton stall), AI-01 (journal "Rettet versjon" shows unverified AI corrections as truth), E-01 (`/eval` ships to prod unguarded → unauth Groq spend — quick win), D-01 (onboarding seed-write race can reset level to A1), R-02 (`/reading` silent below-level on direct visit) + a Low tier. **Pending (not built):** a **dashboard-expression** layout exploration ran (scout brief `.scout/last-brief.md` + 5 structural directions via feature-to-layout) — the research says lead the home with the diagnosis moat as the spine (the "WHOOP move"); awaiting a direction pick before any build.

> **2026-06-22 update — Vision-Contract realignment: p4 verifiers + diagnosis→scheduling + Tier-0/Tier-1 coherence (HEAD `ff3a70c`, all DEPLOYED to pandoai.no; every commit `audit:gate` AUDIT-CLEAN + Rule-9 reconciled + visual-QA'd).** Long session that established a **Vision Contract v1.0** as immutable product law (target user = serious integrator wanting durable daily/work fluency; promise = *produce* Norwegian + steadily *remove the specific things that break your sentences*; the failure real users reported = "a clutter of random Norwegian" → **coherence + motivation** is THE problem, not bugs). Full authority: memory `project_vision_contract_realignment`. Shipped + live:
> 1. **p4 deterministic correction verifiers — 4 classes COMPLETE** (reframed from the infeasible "NB-Llama at runtime" to model-agnostic ordbank-lexicon verifiers): gender (`467b252`) + verb-conjugation (`59b01f8`, `src/lib/verb-forms.ts` + tense-marker check) + adjective-agreement (`0db41eb`, `adj-forms.ts` + determiner-fixes-the-slot) + compound-word/særskrivning (`8f05d29`, `compound-verifier.ts` + lexically-attested-join). All gate AI corrections through the ONE shared `confirmedRepair` (`src/lib/gender-correction-gate.ts`) in conversation + journal + `/skriv`.
> 2. **Diagnosis now STEERS scheduling** (`edcd54d`, `src/engine/scheduler.ts`): when a targeted diagnosis rule fires (confidence ≥0.7) the root-cause + affected concepts LEAD the remediation pool and `recommendedFocus` tilts `resolvePool`. It was computed + shown on the dashboard but the scheduler never read it — the moat's "diagnosis→scheduling" promise was cosmetic; now real (remediation block only; review/new/snakk = T1.6).
> 3. **`audit:gate` hardened to 5 checks** (`207bd1a`, `scripts/ultraaudit.mjs` adds `next lint`): `tsc --noEmit` misses unused-locals but `next build` aborts on them, so a lint error used to pass the gate then break the deploy.
> 4. **Tier-0 (truth):** **T0.1 translate-EN grading recourse** (`0a99780`, Option D) — only ~10 `accepted_answers`/380 EN sentences, so valid paraphrases were marked wrong (a §3.1 lie). Now a non-exact EN answer shows the canonical + lets the learner self-attest ("Ja, jeg forstod") → recorded correct at HALF mastery weight + SRS frozen; the grader is NOT loosened (wrong-meaning still wrong). Plus `/skriv` AI-write gated through `confirmedRepair` + `/shadow` Rule-6 honesty (`e58c558`). **Option B** (`e0c9d55`): hand-authored EN `accepted_answers` for 14 A1 sentences (standing content lane, 83 remain; English alts are eye-verifiable, NOT linguist-gated).
> 5. **Tier-1 (one motivating journey):** **T1.1 dashboard-as-conductor** (`b4e9c3f`, Option C, `src/app/dashboard/page.tsx`) — the dashboard was a menu of competing doors; the first screen is now identity + the SINGLE prescribed action (Dagens-mål hero + coach reason + "Start dagens økt"), and the 5-link "Neste valg" menu + status glance collapse behind one "Mer øving og status" disclosure (/session + /conversation stay one-tap via BottomNav). **T1.3 breaker-story** (`ff3a70c`, `src/lib/breaker-story.ts` + `src/components/progress/BreakerStoryPanel.tsx`) — Progress now leads with "Det som brekker setningene dine" (the learner's real sentence-breakers from the error log, with an HONEST week-over-week trend — "bedre" only when there IS prior-week data) + a "Fikset" milestone (only for genuinely-struggled-then-mastered concepts).
>
> **Process lesson (LOCKED):** VISUAL QA is mandatory — it caught a contrast bug (the recourse card used dark-theme text tokens on the CREAM exercise surface → invisible) that tsc + lint + 760 tests all passed over. Method for any conditional UI state: a THROWAWAY QA route (`src/app/qa-*/page.tsx`) mounting the component with a real corpus id / seeded prop → Playwright screenshot at 375/768/1280/1920 → DELETE the route + `rm -rf .next` (stale `.next/types` referencing the deleted route false-fails `tsc`) before the final gate. **Remaining Tier-1:** T1.4 name-root-cause in-session, T1.5 ladder the production/muntlig cluster, T1.6 diagnosis-steers-ALL-pools, T1.7 journey copy. Then Tier-2 (AI-forward tutor-feel on the deterministic spine).

> **2026-06-21 update — p6 deploy + completeness build (HEAD `e6b97b8`, all DEPLOYED to pandoai.no, `audit:gate` AUDIT-CLEAN, coverage WARNs 8→3).** One long session; every commit gated AUDIT-CLEAN and deployed via the new Operating Rule 9 (reconcile-before-deploy). Shipped + live:
> 1. **The 2026-06-19 p6 build is now DEPLOYED** (supersedes the "NOT yet deployed" note in the box below) — plus this session's work on top.
> 2. **Operating Rule 9 — reconcile-before-deploy** (now also a GLOBAL `~/.claude/CLAUDE.md` non-negotiable): the three trees (working tree / `origin/main` / server) must be clean + a verified fast-forward, gate run ALONE (never concurrent with `npm run build` — `tsc` reads `.next/types` mid-rewrite and false-FAILs), before any deploy. See Operating Rule 9 below.
> 3. **Landing auth-label fix** (`8848309`, `src/app/page.tsx`): a logged-in visitor saw "Logg inn"; label now tracks `useAuth().user` → "Min side".
> 4. **`/listen` A2 honesty** (`715d823`, W5): `getListenQuestions` silently served the A1 set to A2; now an honest below-level banner via `getListenContentLevel` (mirrors roleplay `ff07943`).
> 5. **B1 + B2 cloze passages SHIPPED** (`c136907`, `dd15640`, W2): `content/passages/b1.json` (5) + `b2.json` (5), wired into `src/lib/passage-pool.ts`, linguist-gated, level-honest. Closes the "B1/B2 cloze pending" gap.
> 6. **B2 read→recite→write SHIPPED** (`b40d6c8`, W2): `content/reading/b2.json` (3 passages), `src/lib/reading-loader.ts` now B1+B2; `/skriv` was B1-only, B2 learners now draw B2 via the at-or-below level gate.
> 7. **Diagnosis: closed-class promotion** (`13bb06c`, W4): `modal-verb` / `pronoun-choice` / `negation-placement` promoted to HIGH_CONFIDENCE in `src/lib/classify-error.ts` (clean closed-class swap = same precision as the long-trusted `article-use`; negation OR→AND tightened first). B2 pronoun/modal errors are now diagnosed instead of mislabelled `word-order`. Deterministic. See `docs/decisions/2026-06-21-closed-class-classifier-promotion.md`.
> 8. **Diagnosis: `wrong-word-different-category` via deterministic POS lexicon** (`1108e67`): new `src/lib/pos-map.ts` (Norsk-ordbank CC-BY, 12,441 forms, gender-map pattern); the classifier claims different-category only when both words are known AND their POS sets are DISJOINT (OOV-safe, HIGH_CONFIDENCE). Closes the last diagnostic-tag gap at B1+B2. See `docs/decisions/2026-06-21-pos-lexicon-wrong-word-different-category.md`.
> 9. **Coverage gate made honest** (`4f52c3a`, `913d9fb`, dev-tool only): `CLASSIFIER_COVERED` synced to the expanded HIGH_CONFIDENCE; `noun-gender` credited as derived by diagnosis rule 1 (article-use + adjective-agreement). 
>
> **p6 diagnostic completeness essentially done.** Remaining coverage WARNs (3) are all infra-stub or honest non-gaps: B1 `reading-parsing` (reading-comprehension scoring is a stub), B1/B2 `speed-round` (B2 = honest non-gap, rapid-recall ≠ B2 register). Remaining p6 proper: W3 grammar gates (content). Live tracker: `.planning/STATE.md`.

> **2026-06-19 update — per-level content-quality + moat-visibility build (HEAD `992f61b`, audit:gate AUDIT-CLEAN, pushed to origin/main; NOT yet deployed — origin is ahead of pandoai.no).**
> One long session (/recourse → autopilot → council → md-sync). ~16 commits, every one `npm run audit:gate` AUDIT-CLEAN. Authority for this run: `output/level-gates-plan-2026-06-19.md`, `output/level-coverage-audit-2026-06-19.md`, `.scout/last-brief.md`. Shipped:
> 1. **Diagnosis moat made fully visible (p3 closed).** Dashboard diagnosis depth — `runDiagnosis()[0]`'s focus dimension + affected concepts + a qualitative confidence tier now render in the ProductionWall STATUS half (`47532fd`; `src/lib/production-wall.ts` `deriveDiagnosisHighlight`); the repair card shows a Norwegian **"Fanget · «klasse»"** caught-error chip and the in-session badge a one-line **"why this"** justification tied to recent-miss evidence (`5b21fcf`; `src/lib/selection-justification.ts`). All read-only — no new mastery writes.
> 2. **Per-level Content Contract + standing coverage gate** (`77a1636`; `src/lib/level-contract.ts`): per-CEFR-level difficulty band, remediation floor-relax, allowed exercise types, expected error-tags, grammar gates. `audit:corpus` now WARNs on coverage gaps (concept × level × type × tag) and **credits the high-confidence classifier** classes (article-use/word-order/spelling/compound-word) as effectively-detectable (`b033606`, `992f61b`).
> 3. **Remediate-at-level (the B2-gets-A1 fix).** Multi-skill **Q-matrix** tagging enriches B1/B2 sentence `concept_ids` with the foundational concepts they also exercise (`469b847`, `ad41c24`; `src/lib/concept-detectors.ts`); the seed-path selection now applies a **CEFR ceiling + at-level preference** (`f1f0564`; `src/lib/at-level-select.ts`, wired in `useSession.resolveItem`; the level-gate path got the same ceiling). VERIFIED on the real corpus: a B2 learner weak on noun-gender draws from 127 B2 gender sentences, not 40 A1 ("jeg vil ha kaffe"); an A1 learner's pool stays leak-free.
> 4. **At-level generation validator** (`bcefc61`; `src/lib/level-signals.ts`): deterministic LIX + length + subordinate-clause signals, calibrated on our own corpus (no external lexicon), wired into `validateGenerated` so generated content can't overshoot the learner's level. Gate only — never grades.
> 5. **AI repair-explanation gibberish gated off** (`3dd11ad`): the local 1B (Llama-3.2-1B) produced fluent Norwegian non-words that the length-only guard (and `validateNorwegianOutput`) couldn't catch. `WebLLMService.explainMistake` now returns the deterministic template; the server (Groq 8B) path is gated through `validateNorwegianOutput`. Re-enable on NB-Llama. **Hard rule extended: no unverified AI prose ships to the learner from the weak local model.**
> 6. **B1/B2 listening parity (W1)** (`251de03`, `697ed53`): enabled `listening-comprehension` + `listening-recognition` on +96 B1 / +75 B2 vetted sentences that already have real MP3s (id-derived, `public/audio/sentences/`) — no new content/audio/linguist. Closes the listening skills gap.
> 7. **compound-word (særskrivning) now diagnosable** (`992f61b`): `classify-error.ts` detects compound split/join high-confidence ("kjøkken benk" ↔ "kjøkkenbenk").
> 8. **Roleplay A2 honesty** (`ff07943`): A2 silently served A1 scenarios (Rule-6 violation) → now an honest "Scenarier på A1-nivå" disclosure.
>
> **Remaining p6 (NOT autonomous):** W2 module ladder (B1/B2 cloze, multi-level reading/skriv — linguist-gated content), W4 medium-confidence error tags at B2 (pronoun-choice/modal-verb — a flagged diagnosis-feeding *decision*), wrong-word-different-category (needs a POS classifier). Honest non-gaps left as-is: B2 speed-round, reading-parsing. See `.planning/STATE.md` for the live tracker.

> **2026-06-06 update — live-bug + integrity + moat-visibility sweep (all DEPLOYED to pandoai.no, HEAD `da63374`, 685 tests green, tsc clean).**
> Eight fixes shipped this run, each committed + deployed + verified:
> 1. **Server-side Groq generation is LIVE** (`0e573aa`) — the mobile/Groq path now produces fresh content at parity with desktop WebLLM (was a hardcoded `null` — the phone-path killer). Supersedes the "pending production deploy" note in the Content-dedup bullet below.
> 2. **Corpus integrity: 112 audit ERRORS → 0** (`ec41b09`) — 58 non-canonical B2 error-tags remapped to the canonical-17, 54 `difficulty:4`→3 clamps, 5 dedups. Mechanical, no sentence text changed. The diagnosis moat is now fed only canonical tags.
> 3. **39 B1 word-order keys repaired** (`2c7337c`) — they stored the *scrambled tile string* with literal `/` separators in `norwegian` (clean sentence hid in `notes`); `WordOrderExercise` split on space → unwinnable, forcing phantom word-order errors + SRS resets. Promoted `notes`→`norwegian` (matches the working A2 convention). This closes the `du/deg` / `acceptedOrders`-adjacent word-order residual noted in the 2026-06-03 box (acceptedOrders *population* still deferred).
> 4. **Repair loop no longer serves phantom types** (`930df13`) — `REMEDIATION_EXERCISE_MAP` mapped most tags to `sentence-transformation`/`dictation`; repair micro-drills rendered "kommer snart/skip" instead of remediating. Now filtered via `isNotYetAvailableType` (mirrors the scheduler).
> 5+6. **AI grammar corrections DECOUPLED from the fingerprint** — the Groq 8B produced wrong-but-VALID corrections (live: `jobb`→`et jobb`; *jobb* is masculine) that were shown as truth AND written to mastery, poisoning diagnosis. `validateNorwegianOutput` can't catch a valid-but-wrong-gender form. Council verdict: **suspend unverified AI corrections** — conversation (`36f4b73`, `CONVERSATION_CORRECTIONS_ENABLED=false`) and journal (`cf5141b`, `JOURNAL_ERROR_WRITE_ENABLED=false`) now show feedback + keep deterministic credit but write NO unverified AI error to mastery. **New hard rule: no unverified AI output may move mastery — show, don't grade, until a deterministic check exists.** **Update 2026-06-08 (`467b252`, deployed): the GENDER class now HAS that check.** Lever 3 shipped a deterministic noun-gender verifier (`src/lib/gender-verifier.ts` against the Norsk-ordbank lexicon `src/lib/gender-map.ts`, CC-BY): conversation + journal corrections are re-armed as a per-correction *filter* — only a lexicon-confirmed gender error moves mastery (the live `et jobb` poisoning case is now *rejected*, not written), while V2/conjugation/all other classes stay show-don't-grade until their own deterministic checks exist (→ NB-Llama). The hard rule is upheld, not relaxed.
> 7. **Diagnosis made VISIBLE** (`1165498` + `da63374`) — `runDiagnosis`'s plain-language root-cause `reasoning` was computed every session but consumed only by the scheduler and discarded at the UI; the dashboard coach line showed a generic scheduler string. It is now the coach line (rules 1–4 = cross-concept root cause once error data accrues; fallback = honest weakest-area; Norwegian). **Live-QA'd via Playwright** (seeded fingerprint, 375px) — renders cleanly in the ProductionWall whisper slot, hierarchy holds, 0 console errors. Correctly gated to `wallView`-present (real-data) states.
> 8. **QA-gate agents** — `norwegian-linguist` works when asked to RETURN its report inline (read-only); `architect` returns empty in this OMC env (handoff swallows the body) — produce contract/plan deliverables in the main session, not via `architect`. See memory `project_qa_gate_agents`.
>
> Authority for this sweep: memory `project_engine_moat_gaps` (entries 2026-06-05/06). Remaining levers are now **deferred-by-decision** (deterministic corrector → NB-Llama; MCQ engine → 2nd consumer, see `docs/mcq-engine-contract.md`; `acceptedOrders` population; 3a translate-EN paraphrase) or **operational** (real users — V2 blocked on them).

> **2026-06-03 update — supersedes the earlier "not reliably completable" correction.**
> That earlier correction was based on a walkthrough of a **stale deployed build**. A
> runtime re-verification of HEAD (`1d3beb4`) — local production build + Playwright
> walkthrough (fresh guest: onboarding → 12-Q adaptive diagnostic → dashboard → progress →
> session → forced repair) plus full test suite (645 pass + 1 documented flake) and tsc
> clean — found the locked Phase-1 "Critical" findings **DO NOT reproduce on HEAD**:
> the core session loop **is reliably completable**; the repair loop's "Prøv igjen"
> **re-presents the exact failed sentence** (moat remediation pillar works — verified by
> typing the failed sentence on the retry and having it grade correct + advance);
> translate-to-English grades against `english`; word-order tiles are a proper tap-to-build
> surface with submit gated until arranged; diagnostic commits the level (dashboard + progress
> show it, no A1 fallback); phantom exercise types show honest "kommer snart" banners.
> WebLLM falls back to deterministic templates cleanly (Option C) with no console errors.
> **HEAD deployed to pandoai.no 2026-06-03 — live now matches verified HEAD.** Remaining work
> is minor / non-critical: translate-EN paraphrase rigidity (exact-match), word-order corpus
> doesn't populate `acceptedOrders` + audit one `du/deg` key, repair explanation occasionally
> generic vs the specific error tag. Evidence + verdicts:
> `output/qa-walkthrough-remediation-plan-2026-06-03.md`; live audit in memory
> `project_qa_walkthrough_2026_06_03`. The design bullets below are now runtime-corroborated
> for the core loop; AI-output quality (contract #2) residuals remain template-guardrailed.

The adaptive engine is built but is being stabilized; the following describe intended design:

- **Diagnostic placement** — IRT-style adaptive quiz, seeds the fingerprint (rawScore is pure accuracy: correctCount/attemptCount × 100, confidence 0.4). Cold-start traced and confirmed: new users get real adaptation from session one. Note: DiagnosticResult.rawScore is 0-1 IRT scale, distinct from ConceptMastery.rawScore which is 0-100 EMA scale.
- **Mistake fingerprint** — JSON blob in IndexedDB + Supabase. Per-concept: rawScore, confidenceScore, decayedScore, attempts, uniqueDaysActive, streak, recentOutcomes, SRS state (nextReviewAt, srsLevel). Plus error log (200 cap), error patterns, production gap, speaking minutes, input/production preference.
- **Mastery scoring** — phase-adaptive EMA (α: intro 0.40 → practice 0.25 → consolidation 0.15 → maintenance 0.08), slip detection (a wrong answer after 4/5 recent correct counts at 30% weight), geometric-mean confidence.
- **Decay** — exponential half-life, decays toward floor of 35 (not zero). Half-life is **25 days** (Stream 1.2 shipped); see `src/engine/fingerprint.ts:12`.
- **Calibration window** — first 5 sessions use a 30/30/30/10 recipe variant (Stream 1.3 shipped); see `src/hooks/useSession.ts:188`.
- **Phase model** — locked → intro → practice → consolidation → maintenance, computed live.
- **Scheduler** — recipe 40/30/20/10 (remediation/review/new/interleaving), pulls up to 5 focus concepts (3 weakest by decayedScore + up to 2 SRS-due), SRS-driven review pool, repeat cap (interleaving pool bypasses the repeat cap via `addItem` not `addItemCapped`), production guarantee, Fisher-Yates shuffle. **Weekly Sprint bias (Stream 5):** the 40% remediation pool biases toward `weeklyFocus` via priority ordering (focus concepts first, then weak, then unlocked). **Passed-sentence filtering:** the scheduler consults `passedSentenceIds` and excludes passed sentences from remediation, new-material, and interleaving pools. Review items explicitly allow passed sentences (`excludePassed: false`) for intentional spaced repetition. When all sentences for a concept are passed, the concept is skipped for non-review purposes; AI top-up generates fresh content. Every `SessionItem` carries a required `selectionReason` (`weak_concept` | `review_due` | `decaying` | `new_material` | `interleaving` | `weekly_focus` | `repair_target` | `cold_start`) justifying its inclusion. **CEFR-level sentence filter:** `filterSentencesByLevel` in scheduler.ts ensures sentences are filtered to the learner's current level or below — an A1 learner never sees B1+ sentences even for shared concepts.
- **Weekly Sprint engine (Stream 5)** — `selectWeeklyFocus`, `shouldResetWeek`, `closeWeek`, `openWeek`, `ensureWeekOpen` in `src/engine/weekly-sprint.ts`. Graduation rule promotes/demotes on close.
- **Diagnosis engine** — 4 root-cause rules on real error data. The top rule's plain-language root-cause `reasoning` (Norwegian) IS the dashboard coach line as of 2026-06-06 — the moat made visible (`src/engine/diagnosis.ts` → `src/app/dashboard/page.tsx` → ProductionWall `coachReason`); falls back to the scheduler reason when no rule fires. Rules 1–4 fire once cross-concept error data accrues; the fallback names the weakest evidenced concept earlier.
- **Repair loop** — template explanation + 2 micro-drills + retry + SRS scheduling on the ladder 1→3→7→14→30 days. Wrong answers reset SRS level to 0 (full restart of the ladder).
- **AI validity gate** — all AI surfaces flow through `validateNorwegianOutput` in `src/ai/validate.ts`. Failed validation → per-surface template fallback.
- **Prompt hardening (Stream 1.1 Step 1)** — all 5 prompt builders in `src/ai/prompts.ts` carry Norwegian-enforcement system rules.
- **Event logging (Stream 1.4 writes)** — `src/lib/logEvents.ts` writes anonymized per-exercise and per-weekly-check rows to `learning_events_log` for auth users; guests excluded.
- **Error tagging** — uses sentence's real `error_tags_detectable`, not guessed from exercise type.
- **Content dedup** — no repeated sentence within a session. Cross-session: `passedSentenceIds` prevents passed sentences from reappearing in normal progression. When a non-review item's seed pool is fully passed, `useSession.ts` shows the forced repeat with an honest `isReviewFallback` "Repetisjon" badge instead of silently recycling it (Rule 6), and queues AI top-up. **Server-side generation is now wired** (2026-06-05): `/api/ai` has a `generate` action (`handleGenerate`, Groq JSON mode, mirrors the WebLLM recipe) and `ServerAIService.generateContent` calls it, so the **mobile (Groq) path now produces fresh content** at parity with the WebGPU/WebLLM desktop path. Both paths share `difficultyTier` + `likelySyntheticCompound` (moved to `src/ai/validate.ts`) and gate through `validateGenerated` + `validateNorwegianOutput`; on any failure → `null` → the honest "Repetisjon" fallback (Rule 6). **DEPLOYED to production 2026-06-05** (`0e573aa`) — the mobile/Groq path now produces fresh content live.
- **Full pipeline parity** — session, conversation, journal, **/skriv**, AND weekly check all feed the identical mastery + SRS pipeline. **CAVEAT (updated 2026-06-21, p4 Lever 2):** the old blanket gate (`CONVERSATION_CORRECTIONS_ENABLED` / `JOURNAL_ERROR_WRITE_ENABLED`) is replaced by a per-correction deterministic verifier — the single shared gate `confirmedRepair` (`src/lib/gender-correction-gate.ts`). Conversation, journal AND /skriv write AI-detected errors to mastery, but ONLY when a deterministic verifier confirms the class: **noun-gender** (`verifyGenderCorrection`), **verb-conjugation** (`conjugation-verifier.ts`, tense-marker check), **adjective-agreement** (`adjective-verifier.ts`, determiner check), and **compound-word** (`compound-verifier.ts`, lexically-attested-join check). Every OTHER AI-claimed class (V2, wrong-word, spelling, …) stays show-don't-grade until it has its own check. Deterministic signal (production bricks, speaking minutes, constraint results) feeds as before. **2026-06-21 deep audit fixed a hard-rule violation here:** `/skriv` WRITE step was writing RAW AI corrections to mastery, bypassing the gate — now routed through `confirmedRepair`.
- **Cloze passages (Wave 6.3, shipped + closed 2026-06-02)** — discourse-level fill-in-gap exercise. Per-gap fingerprint write + repair (Rule 8, test-proven), level-honest scheduling (an A1 learner never gets an above-level cloze — test-proven Rule 6), at most one per session. **A1/A2/B1/B2 passages all live** (B1/B2 added 2026-06-21, `content/passages/b1.json` + `b2.json`, linguist-gated; grader is open-input so each gap carries `accepted_answers` for valid alternatives). See `src/lib/cloze.ts`, `src/lib/passage-pool.ts`, `src/components/session/exercises/ClozePassageExercise.tsx`.
- **Production-from-surface (2026-06-02)** — `recordProductionFromSurface` in `src/engine/repair-from-surface.ts`: the correct-production sibling of `repairFromSurface`. One mastery brick on a correct written production; no error logged; productionGap untouched. **Guided/scaffolded frames earn reduced weight via an EMA learning-rate scale** (`learningRateScale` on `updateConceptMastery`, default 1) — scales the step, not the outcome, so a correct answer never lowers mastery; guided frames also freeze the SRS ladder. Built for the read-respond WRITE step.
- **Production wall → "I dag" command card (2026-06-02, committed `d9b28c2`)** — the brick→daily→weekly compounding model made visible, and the dashboard lead block. All-levels, per-level lens via the pure view-model `src/lib/production-wall.ts` (`deriveProductionWallView` + `LENS_CONFIG`: A1 grunnmur / A2 kombinasjon / B1 produksjon / B2 ordforråd — B2 hero reads `vocabCoverage`, not bricks). Component `src/components/dashboard/ProductionWall.tsx`. **Hero is a REAL count, never a %** — production+guided bricks count; recognition/exposure render but never move the hero (the differentiator, test-locked). Bricks = `DailyProgress.bricks {exposure,recognition,production,guided}`, distinguished by FILL+TEXTURE not colour (WCAG AA greyscale), written at the exposure/recognition/production/guided sites. **Merged surface:** the former separate production meter and "Start dagens økt" lime hero are now one card — dark STATUS half (objective + production count + week-sparkline glance + level chip) → lime ACT half (coach-reason whisper + start CTA, whole zone → `/session`) → optional "Produksjonsmur" drawer (full brick wall + legend + labelled weekly sprint + min snakket + gap). Strategic weight hierarchy: two anchors only (the count + "Start dagens økt").

Built features: diagnostic, dashboard (lead "I dag" command card — merged production meter + start-økt CTA — plus WeekStrip + lane strip), session loop, repair loop, conversation mode (AI tutor + constraints + focus bias), journal (focus-biased prompts), reading (concept-tagged texts with exposure logging), progress, profile (with level display + preference toggle), weekly retrieval check at `/uke`, scripted roleplay at `/roleplay` (focus-aware scenarios), read→recite→write production lane at `/skriv` (**B1+B2**; standalone `ReadReciteWriteScreen` + `ReciteStep` + `WriteStep`, deterministic grader, 6 B1 + 3 B2 linguist-gated passages in `content/reading/`, honest below-level gate for A1/A2), shared repair module (`repairFromSurface`), AudioPlayer component (browser TTS fallback), analytics surface (`/analytics`), AlertDialog + Popover primitives, **clickable word-selection popup + universal learner notebook at `/vocab`** (2026-06-24 — save corrections/words from anywhere, promoted items return as deterministic translation-recall on their own SRS).

Retired surfaces: `/recalibrate` REMOVED 2026-06-26 (was a bare redirect to `/uke`, no inbound refs — vision-audit P3 cleanup; old bookmarks now 404 instead of redirecting). (`/vocab` is NO LONGER retired — as of 2026-06-24 it is the **universal learner notebook** home; see the 2026-06-24 update above.)

Live muntlig modes: `/shadow` (shadowing — listen + repeat + word matching), `/drills` (pronunciation drills — 4 sound groups + heuristic feedback), `/listen` (listen-and-respond — 8 questions with focus-biased ordering). All three feed the fingerprint and lane completion system. Dashboard "Muntlig" section links to all three.

In-session exercise types (real): translation (×2), fill-in-blank, word-order, listening-comprehension, speed-round, **cloze-passage** (A1/A2). Phantom types (`sentence-transformation`, `dictation`) route to an honest `NotYetAvailable` banner (Wave 6.6).

Stubs / not built: reading comprehension scoring. (read-respond `/skriv` UI is **shipped**. vocab SRS is NO LONGER a stub as of 2026-06-24 — saved notebook items get their OWN SRS ladder via `src/lib/srs.ts` `advanceNotebookSrs`, independent of `conceptMastery`; see the 2026-06-24 update above.)

## Current Phase

**STABILIZATION — Phase 1 effectively closed on HEAD (2026-06-03).** The "SHIP-READY" claim
stays retired (don't re-assert it without a full fresh audit), but the 7-phase plan's
**Phase 1 (core session engine) is runtime-verified resolved on HEAD**: the walkthrough that
flagged it ran against a stale deploy; a Playwright re-verification of HEAD (`1d3beb4`) plus
green tests (645 + 1 flake) and clean tsc confirmed graders, word-order, repair-retry routing,
diagnostic level commit, and phantom-type honesty all work. **HEAD deployed to pandoai.no
2026-06-03.** Remaining of the plan: minor contract-#1/#2 residuals (translate-EN paraphrase
rigidity; word-order `acceptedOrders` corpus population + one `du/deg` key; repair-explanation
specificity) and Phases 2–7 polish (AI demote/Option C hardening, talkback, journal). Authority +
verdicts: `output/qa-walkthrough-remediation-plan-2026-06-03.md` ("RUNTIME-VERIFIED" section in
memory `project_qa_walkthrough_2026_06_03`). Historical ship-ready context: `docs/vision-and-plan.md`.

**Since ship-ready — Wave 6 exercise pipeline (2026-06-01 → 06-02):** Shipped: cloze passage (6.3, closed with Rule-6 + Rule-8 tests), R0 phantom honesty (6.6), R1 B1/B2 retag (6.7, linguist-gated). Engine/honesty fixes: live `computeProductionGap` + observed-error classifier (`src/lib/classify-error.ts`), Lytt listening-only fix, shadow mistag fix. **read-respond** (read→recite→write skriv module, rows #2+#4): engine helper + deterministic grader + `ReadingPassage` type + 6 linguist-gated B1 passages + **3-page `/skriv` UI — SHIPPED** (`src/app/skriv/page.tsx` → `ReadReciteWriteScreen` + `ReciteStep` + `WriteStep`, B1-only with an honest below-level gate). Not started: B2 nuance-discrimination + conjugation-drill (#3), Nor→Nor transformation (#5), per-level-progression UI. Full status: `docs/vision-and-plan.md` Wave 6.

**What's shipped:**
- Stream 5.5 Phases 3-8 (all surfaces laned, repair externalized, stubs retired, recalibration retired)
- Wave 0: CLAUDE.md sync, `/listen`+`/drills` muted, F027 repair-loop cap
- Wave 1: Audio pipeline — 1,117+ MP3 files generated via edge-tts (nb-NO-PernilleNeural), AudioPlayer wired to real paths with browser TTS fallback. All 4 CEFR levels have audio coverage.
- Wave 2: Analytics surface v1 + moat metric defined + history cap confirmed
- Wave 3: Dashboard composition, progress trajectory, 27 dead CSS classes removed, BottomNav Norwegian
- Wave 4: B1 concept graph (12 concepts), B1 corpus (360 sentences, 30/concept), all 16 files migrated to `getGraphForLevel`, B1 level selectable
- Wave 4.5: B2 concept graph (12 concepts), B2 corpus (360 sentences, 30/concept), B2 audio (360 MP3s)
- Norwegian-dominates pass across ALL learning surfaces (~99% Norwegian)
- Auth: email login is **6-digit/8-digit OTP** (`verifyOtp`, device-independent), NOT magic link. Migrated 2026-06-05 from device-locked PKCE magic link (commits `d4ecdef` + `4e018ec`); `/auth/callback` + `exchangeCodeForSession` kept for future Google OAuth. Production email via Resend SMTP (`no-reply@pandoai.no`). Verified end-to-end cross-device live. See memory `project_auth_redirect_config`.
- Market-readiness audit: security headers, error boundaries (Norwegian), loading skeletons, CEFR sentence filter, API rate limiter (30 req/min), `.env.local.example` credentials cleaned
- **2026-06-05/06 live-bug + moat sweep (all deployed, HEAD `da63374`):** server-side Groq generation live (`0e573aa`); corpus integrity 112→0 audit errors (`ec41b09`); 39 B1 word-order slash-keys repaired (`2c7337c`); repair-loop phantom-type filter (`930df13`); conversation + journal AI corrections decoupled from the fingerprint (`36f4b73`, `cf5141b`); root-cause diagnosis surfaced as the dashboard coach line + Norwegian + tightened, Playwright-QA'd (`1165498`, `da63374`); B1 corpus deepened to ~51/concept earlier in the run. See the 2026-06-06 box under **Current State** for the full account.
- **2026-06-08 — Lever 3: deterministic noun-gender corrector (`467b252`, deployed to pandoai.no, `audit:gate` AUDIT-CLEAN / 698 tests).** Re-arms the gated-off conversation + journal corrections for the gender class only, via `verifyGenderCorrection` (`src/lib/gender-verifier.ts`) against a Norsk-ordbank lexicon (`src/lib/gender-map.ts`, CC-BY, generated by `scripts/build-gender-map.ts`). Two-gender nouns stored as a bit-union so "en bok" never false-flags; the `et jobb` poisoning case is rejected. Part of the four-lever stabilization program (Lever 1 hygiene/gate done; Lever 2 NB-Llama deferred — does not gate this; Lever 4 real users, parallel). See `.planning/STATE.md`.

**Next (operational, not code):** ~~Rotate Supabase keys~~ (done 2026-05-29), ~~deploy to pandoai.no~~ (live), ~~auth completion~~ (done — OTP login shipped + verified e2e 2026-06-05). Remaining operational: Google OAuth one-tap (optional; `/auth/callback` already retained for it). V2 engine (Wave 5) blocked on real users.

## Operating Rules (HARD RAILS — these caused real problems when absent)

1. **Depth, not breadth.** Do not add feature surface. Finish and harden the current phase before proposing anything new. Adding features while foundations are open is the documented failure mode of this project.

2. **Analysis-first on decisions; direct on mechanics.** If a task involves a reversible architectural or design choice, produce an analysis with 2–3 options and tradeoffs and STOP for approval. If a task is mechanical and well-understood, do it directly and show the diff. Do not run analysis loops on one-line fixes; do not build architectural changes without analysis.

3. **Verify, don't assume.** Before declaring anything done, produce a concrete trace against stated acceptance criteria. "It should work" is not verification. A wrong audit was caught this way; conversation parity was confirmed this way. This is mandatory. **Verification must include a returning-user fingerprint, not only a fresh guest** — a fingerprint persisted under an older schema is the real production population, and guest-only live smoke shipped a returning-user dashboard crash on 2026-06-03. The locked guard is `tests/integration/returning-user-read-safety.test.ts` + `tests/types/returning-user-fixture.ts`: any read surface that assumes a field a legacy fingerprint lacks must fail there. When adding a field to `MistakeFingerprint`, confirm `normalizeFingerprint` (`src/types/fingerprint.ts`) backfills it and extend the fixture.

4. **One move at a time.** Finish the current move, verify it, summarize what changed, then stop. Do not chain into the next move unprompted.

5. **Surface drift, don't route around it.** If code conflicts with this file or a spec, or if a request seems to contradict the moat or north star, raise it as an explicit question. Do not silently reinterpret the request to make it fit.

6. **No silent substitution.** Never make a feature appear to work when it doesn't (the old B1/B2-runs-A2 pattern). Honest banners over silent fallbacks.

7. **Scope discipline on prompts.** If asked to fix X, fix X. Do not also refactor Y, restyle Z, or build the thing you think should come next. Note adjacent issues; don't act on them without approval.

8. **Pipeline honesty.** If a surface claims to contribute to the fingerprint, mastery, or repair loop, that contribution must be traced end-to-end before the surface ships. A UI that shows a correction card is making a claim about the learning engine, not just about pixels. Five separate surfaces during P0 recovery were found to silently contribute nothing: the AI badge, error tags, session progression, journal correction, and conversation grammar logging. Before any "feeds the engine" feature ships, trace the write: confirm an entry lands in the error log, the mastery score changes, or the SRS state updates — in a real session, not in theory.

9. **Reconcile before deploying — never ship an unreconciled state.** Before any deploy, the three trees must be clean and consistent: (a) the **working tree** has all intended work committed — no stray uncommitted or stashed changes that belong in this deploy; (b) **`origin/main`** reflects exactly what's meant to ship — everything pushed, nothing half-landed or divergent; (c) the **server** is a clean fast-forward of `origin/main` — verify the currently-deployed commit is an ancestor of the target HEAD (`git merge-base --is-ancestor <server-HEAD> HEAD`), so the pull only moves forward. Run `bash ~/.claude/scripts/git-reality-check.sh` and `npm run audit:gate` **alone** (never concurrently with `npm run build` — tsc reads `.next/types` mid-rewrite and false-FAILs type-safety) first. If any tree is dirty, divergent, behind in an unintended way, or the gate fails, **abort and reconcile before deploying** — do not deploy around it. This rule exists because a deploy was found running 16 commits behind origin with stale stashes lying around and a transient false-fail gate.

## The Architect Subagent

This project has an architect subagent at `.claude/agents/architect.md`. Its job is direction, sequencing, and pushback — NOT writing code. Consult it before starting any phase, when a request might be breadth-not-depth, when scope is unclear, or when a decision has architectural weight. The architect proposes and challenges; the main session executes after approval. Treat its scope/direction flags as blocking until resolved with the user.

## Documents That Outrank This File On Their Specific Domains

- `docs/vision-and-plan.md` — the unified vision, ship-ready definition, dependency map, and wave-ordered execution plan. The forward-looking source of truth for what to build next.
- `docs/roadmap.md` — the historical build record with per-phase closure notes, research citations, and procedural locks.
- `docs/muntlig/architecture.md` — the full muntlig module architecture (audio, pronunciation, content, branching, timing). When muntlig is built, this is the spec.
- `docs/validation-and-research.md` — the research findings on model quality, SRS, decay, mastery, cold-start, competitive position. The reasoning behind the engine corrections lives here. Read this if you wonder why a decision was made.
- `docs/ui-1/` — aesthetic direction, token contract, typeface analysis, shadcn audit, type scale. The UI phase's source of truth.

If those docs conflict with each other or with this file, surface the conflict — don't pick.

## When in Doubt

Re-read the moat and the north star above. Every decision should trace to one of them. If it doesn't, it's probably out of scope — raise it, don't build it.

## How to Start a Session

1. Read this file fully.
2. Read `docs/roadmap.md` for the current sequence.
3. State the current phase and what's in scope.
4. For anything non-trivial, propose a plan and stop for approval (Operating Rule 2).
5. Do not ask the user to re-explain the project. It's all here.
